name: <Agent display name>
slug: <agent-slug>
description: Agent workspace managed by the agent-builder skill.
labels:
  - agent-builder
registerWorkspace: false
config:
  value: {}
  schema: {}
secrets:
  schema:
    agentFactoryApiKey:
      type: string
      title: Agent Factory API Key
      description: Governance org API key (iak_...) used by the Agents app.
      ui:widget: password
customDomains: []
repositories: {}
