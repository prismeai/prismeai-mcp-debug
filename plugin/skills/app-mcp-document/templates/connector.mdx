---
title: '<<SERVICE_NAME>>'
description: '<<ONE_LINE_DESCRIPTION>>'
---

<<HEADER_ICON>>

<<INTRO_PARAGRAPH>>

<<FEATURE_CARDS>>

## Who is this for?

This connector is used by three different roles. Jump to the section that matches yours — each one is self-contained.

<CardGroup cols={3}>
  <Card title="Agent builder" icon="robot">
    You build agents in **Agent Factory** and want them to use <<SERVICE_NAME>>. → *Agent builder* tab.
  </Card>
  <Card title="Platform admin" icon="shield-halved">
    You run the platform and set up <<SERVICE_NAME>> once for everyone. → *Platform admin setup* accordion below.
  </Card>
  <Card title="Workspace builder" icon="puzzle-piece">
    You write Builder automations (DSUL) that call <<SERVICE_NAME>> operations directly. → *Workspace builder* tab.
  </Card>
</CardGroup>

## Prerequisites

<<PREREQUISITES>>

<Accordion title="Platform admin (Governance) — one-time platform setup" icon="shield-halved">
  <<PLATFORM_ADMIN_SECTION>>
  {/* Closed by default — most readers (agent / workspace builders) skip it; platform operators expand it once.
      Split into two `## ` sub-sections when both apply:
      `## 1. Configure the connector` and `## 2. Declare the capability in AI Governance`.

      Static-credential archetype: a <Note> — "No platform-level setup. Each workspace pastes its own credential in the app config." (no section 2 unless a shared capability is offered).
      Central-OAuth via Governance: §1 register OAuth app → store secrets in core ws; §2 attach the Governance auth-config JSON (status/connect/disconnect webhooks) → grant roles → smoke-test.
      Tenant-context + config SPA (generic connector, e.g. google-workspaces): §1 register OAuth app → enter creds via the CORE workspace's config app (`<studio>/apps/<slug>`, NOT raw Studio Secrets) → tell tenants to pick `oauthCentral`. §2 (generic connectors only) declare the named capability in AI Governance: MCP endpoint URL + Scope `context_id,agent_id,user_id` → it then appears in the agent builders' capability picker (catalog access follows org RBAC; there is NO per-capability role grant for tenant-context connectors — do not write a "grant the capability to roles" step here, that gate only exists for Central-OAuth) + smoke-test. End with a <Warning>: declaring the capability ≠ authorizing an agent — per-agent gating is the config-app allowlist; no OAuth auth-config JSON here.
      Webhook bridge: <Steps> to register the bot/webhook at the provider. */}
</Accordion>

---

<Tabs>
  <Tab title="Agent builder (Agent Factory)">
    ## Agent builder
    {/* This `##` is intentional: Mintlify excludes tab *labels* from the "On this
        page" TOC but includes headings inside the active tab. Opening each tab with
        its audience heading is what surfaces "Agent builder" / "Workspace builder"
        in the right-hand TOC. Keep it short (not the full parenthetical label). */}

    **Goal:** let an agent you build in Agent Factory use <<SERVICE_NAME>> through MCP tools.

    <Note>
      Before an agent can call the connector, a *Workspace builder* must have installed and configured the <<SERVICE_NAME>> app (see the *Workspace builder* tab)<<ADMIN_DEPENDENCY_CLAUSE>>.
    </Note>

    <<AGENT_BUILDER_WIRING>>
    {/* If a catalog capability exists (generic connector — Platform admin §2 declared one), split into two `## ` sub-sections:
        `## Option A — Enable the shared capability from the catalog` (easy: pick it from the catalog, pre-wired endpoint+Scope, NO allowlist step — the shared instance accepts every agent granted the capability; <Note> it's a shared platform-managed instance whose Google credentials are owned by someone else), and
        `## Option B — Run it from your own workspace (recommended)` (install the connector in YOUR workspace, point the agent at YOUR MCP endpoint + Scope `context_id,agent_id,user_id` + allowlist). Lead Option B with a <Warning> that this is the SECURITY-preferred mode: credentials/tokens/allowlist scoped to your workspace, least-privilege isolation vs the broad shared surface.
        If NO catalog capability: a single <Steps> — add the capability pointing at the MCP endpoint with Scope `context_id,agent_id,user_id` (+ allowlist for tenant-context). Never document `mcp-api-key` (deprecated); mention it only as a one-line legacy note if the connector strictly still requires it.
        Always finish with a shared `## Brief the agent in its system prompt` block. */}

    <Note>
      **Legacy AI Knowledge agents** (no native MCP picker): add the connector under **Advanced > Tools > MCP**, paste the **MCP Endpoint** URL<<LEGACY_KEY_CLAUSE>>.
    </Note>

    ## Available Tools

    <<MCP_TOOLS_TABLES>>

    ## Output Formats

    <<OUTPUT_FORMATS>>

    ## Tool Details

    <<TOOL_DETAILS>>
  </Tab>

  <Tab title="Workspace builder (DSUL)">
    ## Workspace builder

    ## Installation

    1. Go to **Apps** in your workspace
    2. Search for **<<SERVICE_NAME>>** and install it
    3. <<INSTALL_CONFIG_STEP>>

    ## Configuration

    <<CONFIG_TABLE>>

    ## Available Instructions

    Every instruction resolves credentials from the workspace configuration.

    <<INSTRUCTIONS_TABLES>>
    {/* One `### Category` + table per resource group, columns: Instruction | Description | Returns.
        Description = what the op does, folding the key/required inputs inline (e.g. "…by `fileId`", "`body` = {…}") — do NOT dump a bare arg-name list, it is low-value.
        Returns = the output shape (the underlying provider resource), e.g. `{ files: [{ id, name }], nextPageToken }` or "Empty (HTTP 204)". */}

    <Note>
      `Returns` shows the shape of the operation output.
    </Note>

    ## DSUL Examples

    <<DSUL_EXAMPLES>>
  </Tab>
</Tabs>

---

## Error Handling

<<ERROR_HANDLING_TABLE>>

### Common Issues

<<COMMON_ISSUES>>
{/* Tailor to the archetype (see SKILL.md): tenant-context → allowlist / missing agent_id scope / not-connected / dead refresh token / OAuth not configured;
    central-Governance → auth required / central OAuth config missing / capability not granted; static-credential → Not configured / Invalid credentials.
    Never use `mcp-api-key` wording (deprecated). Always end with one service-specific gotcha. */}

## External Resources

<<EXTERNAL_RESOURCES>>
